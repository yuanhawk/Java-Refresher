package MathMethod;

public class MathMethods {

	/* java.lang.Math
	 * round(num) - returns closest long to double / closest int to float, no dp
	 * pow(num1, num2)
	 * sqrt(num)
	 * max(a,b) - returns larger of the 2
	 * min(a,b) - returns smaller of the 2
	 * random() - returns double between [0.0, 1.0)
	 */
}
