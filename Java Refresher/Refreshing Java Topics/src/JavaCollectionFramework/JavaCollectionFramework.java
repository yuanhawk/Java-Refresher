package JavaCollectionFramework;

public class JavaCollectionFramework {
	
	/* Collection - hold >= 1 obj
	 * >Set: Unordered collection of unique obj
	 * HashSet - Stores a set of unique elements
	 * >List: Ordered collection of obj (order of added item/sorted based on key value), allows duplicate elements, access via index
	 * ArrayList - Efficient in accessing individual elements randomly, less efficient in inserting in the middle
	 * LinkedList - Efficient way to inserting in the middle of a list, but less efficient when accessing elements
	 * 
	 * Map - similar to collection, but uses key-value pairs, 1 key (identity) - 1 value (storage)
	 * HashMap - stores key-value pairs, no duplicate key, but allows duplicate value
	 * TreeMap - stores k-v pairs in a hierarchial data structure (tree), automatically sequences elements by key
	 */

}
