package StringMethod;

public class StringMethod {
	
	/* equals(String) - compare equality of value
	 * equalsIgnoreCase(String) - compare equality of value, not case-sensitive
	 * isEmpty() - returns bool
	 * startsWith(String) - returns bool
	 * endsWith(String) - returns bool
	 * 
	 * String indexes
	 * length() - returns int value of this length of the String
	 * indexOf(String) - returns value of 1st occurrence
	 * indexOf(String, startIndex) - returns 1st occurrence of String starting at specified index
	 * lastIndexOf(String) - returns value of last occurrence
	 * lastIndexOf(String, startIndex)
	 * charAt(index) - returns char value at specified index
	 * 
	 * trim() - removes spaces from the beginning and end of the String
	 * subString(startIndex) - returns String object fr specified index to the end, similar to Python Slicing
	 * subString(startIndex, endIndex)
	 * 
	 * Strings are immutable, but if you want flexibility use StringBuilder
	 */

}
